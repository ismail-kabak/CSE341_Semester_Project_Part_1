-- Program 2: user-defined function + GROUP BY + aggregate functions
table sales = load "sales.csv"

function summary(t: table) -> table {
    return SELECT region, COUNT(*), AVG(amount)
           FROM t
           GROUP BY region
           ORDER BY region ASC
}

let report = summary(sales)
print report
