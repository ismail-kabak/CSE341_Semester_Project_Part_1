// Cat 2: quantity_of() with no operand — must fail (§2)
recipe test() serves 1 {
    ingredient flour : 200 g
    step "x" {
        let q : Mass = quantity_of()
        combine(flour)
    }
}
evaluate test()
