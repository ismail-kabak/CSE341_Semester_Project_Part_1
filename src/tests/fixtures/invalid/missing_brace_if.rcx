// INVALID: missing braces on if body (Decision #20)
// Expected parse error at line 6: expected '{' after 'if' condition

function check(n: int) -> bool {
    if n > 0
        return true
    return false
}

evaluate check(1)
