// Cat 4: a < b == c — different precedence levels, NOT non-associativity.
// Parses as (a < b) == c which is perfectly legal (decision #17).
// Must succeed.
function f(a: int, b: int, c: bool) -> bool {
    return a < b == c
}
evaluate f(1, 2, true)
